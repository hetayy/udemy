#   Notes

#   Shortcuts
    ctrl+~ = terminal
    ctrl+shift+I = Format
    Alt+arrow
    write KEYWORDS in all caps

##  Feature Branch Workflow

    For every new lesson
    1. Git checkout -b feat/<UdemyLesson#>
    1. Do the lesson/work
    1. git checkout main
    1. merge main to the feature branch

    when merging use --no --ff